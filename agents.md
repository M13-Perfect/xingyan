# Role: Senior Systems Architect & Security Expert (Codex Agent)

## Current Repo Handoff (2026-06-25)

- Read `PROJECT_INDEX.md` first, then `CURRENT_TASKS.md`, before opening source files.
- Verified backend: `xyyx/` is Spring Boot 3.4.3 + Java 17 + MyBatis + MySQL.
- Verified active frontend after upstream pull: `xyyx/xyyx-frontend/` is Vue 3 + Vite and calls `/api` through the Vite proxy.
- `XinghanApp/` was removed by upstream commits; do not treat it as the active frontend.
- SQL exists at repo root: `xyyx_db.sql` and `admin.sql`. `admin.sql` contains local credential seed data; do not quote values in responses.
- No Redis layer exists. Do not add Redis before authorization, data model cleanup, and service boundaries are stabilized.
- Casdoor SSO implementation exists locally: Spring Resource Server protects `/api/**`, `/api/me` maps JWT `preferred_username` or Casdoor `name` to local `user.username`, and local `user.role` remains the business role source.
- Vue frontend uses Authorization Code + PKCE S256 and injects `Authorization: Bearer <access_token>` for `/api` calls.
- Local Casdoor Docker runtime exists at `deploy/casdoor/`; it uses `casbin/casdoor:latest` with SQLite and exposes port `8000`.
- On 2026-06-22, LAN discovery/JWKS were verified at `http://192.168.1.29:8000`, backend ran on `8080`, and frontend ran on `0.0.0.0:5173`; re-check LAN IP and discovery before reusing these values.
- 2026-06-29 local runtime re-verified on LAN IP `192.168.1.29`: Docker Desktop daemon must be running before `deploy/casdoor/docker compose up -d`; `xyyx-casdoor` served discovery/JWKS `200` on `8000`, Spring Boot served `GET /api/security/public-key` `200` and expected pre-login `GET /api/me` `401` on `8080`, and Vite served `/` `200` plus proxied `/api/me` `401` on `5173`. For Windows background starts, prefer `SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI` / `SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI` env vars over a space-containing Maven `-Dspring-boot.run.arguments` string.
- 2026-06-30 local restart wrapper: run root `restart-xingyan.bat` to start Casdoor via Docker Compose, stop listeners on `8080`/`5173`, and launch backend/frontend with matching `http://192.168.1.29` Casdoor/frontend URLs. Pass a LAN IP as the first argument if the machine IP changes.
- 2026-06-22 SSO foundation: frontend PKCE keeps S256 when `crypto.subtle` is unavailable by using an internal SHA-256 fallback while still requiring `crypto.getRandomValues`; the old main-window Casdoor authorize/login behavior below was superseded by the 2026-06-25 business-owned `/login` refactor.
- 2026-06-25 auth entry refactor: unauthenticated users now stay on business `/login`; protected paths rewrite to `/login?returnTo=<internal path>`. The SPA no longer navigates the main window to Casdoor HTML pages. `src/auth/oidc.js` stores a 10-minute one-shot PKCE S256 transaction in `sessionStorage`, `src/App.vue` posts the business login form to Casdoor `/api/login` with the existing OIDC code context, then exchanges the returned authorization code. No client secret and no password grant are used.
- 2026-06-25 Headless Login 401 fix: `src/auth/oidc.js` now sends Casdoor-compatible PKCE keys `code_challenge_method=S256` and `code_challenge=<value>` instead of the rejected camelCase `challengeMethod` and `codeChallenge`; `src/auth/oidc.test.js` locks this with a regression test. Do not fix this class of 401 by opening `/api/**`, skipping JWT validation, or returning mock users.
- 2026-06-25 login UI simplification: `xyyx/xyyx-frontend/src/App.vue` now uses a quiet centered `/login` form with explicit labels and copyright footer. It intentionally shows no customer list, customer preview, phone/project data, or "only responsible customers" claim on the unauthenticated screen; keep future login UI similarly data-free.
- 2026-06-25 app UI simplification: `xyyx/xyyx-frontend/src/App.vue` now adds a password visibility toggle on `/login` and restyles the authenticated workbench to match the same quiet light theme. Browser visual QA used local mocked `/api` responses for layout only; do not treat that screenshot as real backend or Casdoor acceptance.
- 2026-06-25 responsive topbar fix: `xyyx/xyyx-frontend/src/App.vue` now shows a desktop horizontal nav at `min-width: 901px` and keeps the hamburger menu for `max-width: 900px`; the old unused `isMobile` / `checkDevice` width detector was removed. Browser QA used a temporary local `/api` mock only for layout verification.
- 2026-06-26 workbench create-record UI: `xyyx/xyyx-frontend/src/App.vue` no longer renders the survey create form inline above the table. The workbench shows one `+ 录入新回访记录` button, opens the existing field set in a modal, closes the modal after successful `submitAdd`, and widens `.btn-close` so Chinese text does not wrap. Verified with frontend `npm test`, `npm run build`, and in-app Browser visual QA against temporary local mocks only.
- 2026-06-25 survey social-account field: customer records now have optional `survey.social_account` / `Survey.socialAccount`; `POST /api/surveys` accepts `socialAccount`, `SurveyMapper` persists it and includes it in keyword search, and `src/App.vue` shows it in the create form, list contact block, and directly below `城市` in the detail modal. Existing DBs need `ALTER TABLE survey ADD COLUMN social_account varchar(100) DEFAULT NULL AFTER wechat;`. No unique index was added. Verified with `mvn test`, frontend `npm test`, `npm run build`, and in-app browser check at `http://127.0.0.1:5173/`.
- 2026-06-22 Casdoor user acceptance: local Casdoor now has business organization `xyyx`, application `xyyx` with client ID `xyyx-web`, redirect URI `http://192.168.1.29:5173/auth/callback`, and test user `18007300157`. XYYX org password complexity was relaxed locally to allow this temporary short-password account; do not copy that policy to production.
- 2026-06-22 token-backed acceptance passed: clean-browser login from `http://192.168.1.29:5173` reached `http://192.168.1.29:8000`, entered the system, and `/api/me` returned HTTP 200 for `18007300157` with local DB role `admin`; `scripts/acceptance/Verify-CasdoorSso.ps1` passed 23/23 with a real access token.
- Deployment notes: `docs/deployment/casdoor-sso.md`; acceptance script: `scripts/acceptance/Verify-CasdoorSso.ps1`; Chinese acceptance notes: `docs/acceptance/casdoor-sso-acceptance.md`.
- Known high-risk gaps: Casdoor app/user data must keep matching local `user.username`, production must restore stronger password policy and HTTPS, broad CORS review, Casdoor MFA/consent/prompt states currently stop on the business login page and require admin handling, SurveyController doing service/data-access work directly, physical survey delete, index-hostile survey search/sharing.
- 2026-06-26 database compatibility upgrade: no migration runner exists, so manual versioned SQL was added under `db/migrations/` with verification SQL under `db/verify/` and operator docs under `docs/database/`. `xyyx_db.sql` is local full bootstrap and now includes the sales-workflow schema, but it still contains `DROP TABLE IF EXISTS`; never promote it to production migration. `V20260626_001_expand_sales_workflow.sql` only adds columns/indexes/tables and intentionally avoids first-round foreign keys. `V20260626_002_backfill_sales_workflow_compat.sql` backfills owner/user IDs and `user_role` from legacy `user.role`. Local MySQL `xyyx_db` was upgraded and verified on 2026-06-26. `SurveyMapper` survey list queries now use explicit columns instead of `SELECT *`, guarded by `SurveyMapperCompatibilityTests`.
- 2026-06-26 employee create compatibility fix: `SurveyController.addUser` now runs in a transaction, only accepts staff creation, inserts the legacy `user` row, and immediately inserts the active `user_role` row through `sys_role.code = 'STAFF'`; deleting staff now revokes active `user_role` rows before deleting the legacy user. Guarded by `SurveyControllerUserManagementTests`. Browser QA was non-destructive only; no temporary employee was submitted to the live DB.
- 2026-06-28 Baota deployment packaging: current upload artifacts live under `release/`: `xyyx-backend-baota.zip`, `xyyx-frontend-dist.zip`, and `xyyx-baota-nginx.zip`. Backend must use external config `application-baota.properties` via `--spring.config.additional-location=...`; do not bake DB or Casdoor secrets into the JAR or source files. Frontend Vite output currently compiles Casdoor URL at build time, so rebuild with production `VITE_CASDOOR_BASE_URL` and `VITE_CASDOOR_REDIRECT_URI` before real SSO deployment unless a deliberate same-origin reverse-proxy plan is used.
- 2026-06-28 production domain packaging: `xyyx/xyyx-frontend/dist` and `release/xyyx-frontend-dist.zip` were rebuilt for `VITE_CASDOOR_BASE_URL=https://sso.xujie.xin`, `VITE_CASDOOR_REDIRECT_URI=https://xujie.xin/auth/callback`, and `VITE_CASDOOR_CLIENT_ID=xyyx-web`. Full Nginx examples are in `release/nginx/sso.xujie.xin.full.conf` and `release/nginx/xujie.xin.full.conf`, zipped as `release/xyyx-nginx-full-conf.zip`. Keep `/.well-known/openid-configuration` and `/.well-known/jwks` reachable through the SSO host.
- 2026-06-28 deployed Network Error fix: live probes showed the deployed frontend bundle uses `https://sso.xujie.xin`, `https://xujie.xin/auth/callback`, and `xyyx-web`; the actual failures were infra. First, `xujie.xin:443` presented a cert whose SAN was only `DNS:www.xujie.xin`, while `www.xujie.xin:443` presented the `sso.xujie.xin` cert. A Let's Encrypt cert for `DNS:xujie.xin` + `DNS:www.xujie.xin` was issued through Baota `acme_v2`, installed under `/www/server/panel/vhost/cert/xujie.xin/`, and Nginx was reloaded. Second, Casdoor returned 502 because container `casdoor` could not mount `/www/wwwroot/casdoor/conf/app.conf` (it was an empty directory) and then could not write `/logs/casdoor.log`; restored the app.conf file from `/www/wwwroot/xyyx-source/conf/app.conf`, set `/www/wwwroot/casdoor/logs` owner to `1000:1000`, and restarted the container. Current verification: `https://xujie.xin/api/me` returns expected pre-login `401`, and SSO discovery/get-app-login return `200` with CORS. Follow-up `user doesn't exist` was fixed by syncing the existing business user `18007300157` into Casdoor owner `xyyx` using the business DB's existing BCrypt password hash; no plaintext password was read or printed. A wrong-password probe now returns `password or code is incorrect`, and the probe failure counter was reset. A later post-credential `Network Error` was fixed by adding `proxy_hide_header Access-Control-Allow-*` to production `sso.xujie.xin.conf`; token POST now returns a single `Access-Control-Allow-Origin: https://xujie.xin` instead of duplicate empty + valid origins. `release/server-issue-xujie-cert.py` preserves the cert repair steps.
- 2026-06-28 VPS business data import: imported the user-provided dump `xyyx_db_2026-06-27_12-37-40_mysql_data_nxNKg.sql` as data-only. The dump DDL was filtered out; `survey` and `user` `INSERT`s were converted to `INSERT IGNORE` with explicit legacy column lists, so newer VPS columns were left to defaults. Schema hash before/after stayed identical. Counts changed `survey: 0 -> 275`, `user: 1 -> 13`. VPS backup paths are `/www/backup/xyyx-db/xyyx_db-before-data-import-20260628203524.sql` and `/www/backup/xyyx-db/xyyx_db-after-data-import-20260628203641.sql.gz`.
- 2026-06-28 phone privacy module: active customer data is still the `survey` table, not a separate `customer` table. New writes stop storing plaintext `survey.phone`: `SurveyController.addSurvey` normalizes raw phone through libphonenumber, writes AES-256-GCM fields (`phone_ciphertext`, `phone_iv`, `phone_tag`, version), HMAC-SHA256 `phone_hash`, `phone_suffix4_hash`, `phone_mask`, and leaves legacy `phone` NULL. `PhonePrivacyService` reads Base64 32-byte keys only from env/private config (`XYYX_PHONE_ENC_KEYS`, `XYYX_PHONE_HASH_KEYS`, version vars); no key material is committed. `SurveyMapper` default list/search selects only `phone_mask AS phoneMask`; exact and suffix4 phone search use POST endpoints and tenant+HMAC indexes. `POST /api/customers/{customerId}/phone/reveal` requires JWT user, row visibility, `PHONE_VIEW_FULL` in `role_permission`, non-empty reason, writes `personal_info_access_log`, and returns the full phone for 60 seconds. Frontend stores revealed phones only in memory and never in URL/sessionStorage/localStorage/console. Migrations: run `V20260628_001_add_phone_privacy.sql`, backfill old `survey.phone` rows with production keys, verify coverage, then run `V20260628_002_clear_legacy_plain_phone.sql`; `xyyx_db.sql` is bootstrap only. Verified with `mvn test` (16/16), frontend `npm test` (7/7), and `npm run build`.
- 2026-06-29 phone search/backfill repair: 回访记录 search is back on `GET /api/surveys?keyword=`. `SurveyController.resolveSurveySearch` maps valid full phone input to `phone_hash`, 4 digits to `phone_suffix4_hash`, ordinary keywords to non-phone fields only, and legacy digit-like phone input to the same `LEGACY_DIGITS_V1` hash used by opt-in backfill; `phone LIKE` is still forbidden and new survey writes still use strict libphonenumber validation only. `xyyx/xyyx-frontend/src/App.vue` no longer calls `/api/customers/search/phone*` from the回访页 search box, sanitizes survey rows before state storage, and alerts on `fetchData()` API errors. New backfill command: run Spring Boot with non-option arg `phone-privacy-backfill` and options `--table=survey --tenantId=<tenant> --batchSize=500 --dryRun=true|false --legacyDigits=true|false`; it selects only `phone IS NOT NULL AND phone_hash IS NULL`, updates with `WHERE id = #{id} AND phone_hash IS NULL`, logs only `surveyId` plus error code/aggregate counts, and closes the Spring context after one-shot execution. Added `db/verify/V20260628_preflight_phone_privacy.sql` for missing-column preflight and `db/verify/V20260628_verify_phone_privacy.sql` for post-V001 coverage. Local V001 was executed after approval and all 12 columns are present. Initial real `--dryRun=false --legacyDigits=true` backfill completed with `scanned=53`, `updated=53`, `invalid=0`, `legacy=52`, and coverage is `hashed/masked/encrypted=53`, `need_backfill_rows=0`, but fresh verification found the current User-scope `XYYX_PHONE_*` env vars were missing. New formal local User-scope keys were generated without printing or committing values; these current keys do not match existing hashes (`exact_hash_match_rows=0`, `suffix4_hash_match_rows=0`). Phone search acceptance still requires approved rekey/re-encrypt from legacy `survey.phone`. V002 remains pending and must not be run before search acceptance.

- 2026-06-29 phone rekey status, supersedes older key-mismatch note: ordinary User-scope `XYYX_PHONE_*` keys are set, controlled local `phone-privacy-backfill --rekey=true --confirmRekey=true --dryRun=false --legacyDigits=true` completed with `scanned=53`, `updated=53`, `invalid=0`, `legacy=52`, and current key verification returns `exact_hash_match_rows=1`, `suffix4_hash_match_rows=1`. V002 remains pending and must not be run before final API/search acceptance.

- 2026-06-29 phone rekey status, supersedes older key-mismatch note: ordinary User-scope `XYYX_PHONE_*` keys are set, controlled local `phone-privacy-backfill --rekey=true --confirmRekey=true --dryRun=false --legacyDigits=true` completed with `scanned=53`, `updated=53`, `invalid=0`, `legacy=52`, and current key verification returns `exact_hash_match_rows=1`, `suffix4_hash_match_rows=1`. V002 remains pending and must not be run before final API/search acceptance.

- 2026-06-30 phone reveal session implementation: added additive manual migration `db/migrations/V20260629_001_phone_reveal_session.sql` and verify SQL `db/verify/V20260629_verify_phone_reveal_session.sql` for `phone_reveal_login_session`, audit extension fields, and historical reveal-policy compatibility objects. The runtime now prefers JWT `sid`, `sessionId`, or `jti`, and falls back to a server-side token fingerprint made from the JWT token/iat/exp without storing or logging the raw JWT. Survey lists decrypt only the current returned page when the global setting allows session visibility and the current user/session is activated; rows with missing privacy fields degrade to masked display rather than failing the whole page. `SurveyMapper` ordinary backfill selects/updates rows when any required privacy field is missing (`phone_hash`, `phone_mask`, ciphertext, IV, tag, suffix4 hash). Operator notes: `docs/database/phone-privacy-operator.md`. DB write commands were not run without approved credentials.
- 2026-06-30 single reveal 500 repair: canonical click path is `POST /api/surveys/{surveyId}/phone/reveal`; frontend sends `{}` through the axios bearer-token interceptor, has no `window.prompt`, and uses in-flight/5s cooldown. `getApiErrorMessage` now displays backend JSON `message/code/requestId`. Backend reveal validates privacy structure before decrypt: ciphertext non-empty, 12-byte IV, 16-byte tag, nonblank key version, tenant ID, and customer UUID. Incomplete rows return JSON `409 PHONE_PRIVACY_NOT_READY`; decrypt failures return JSON `500 PHONE_DECRYPT_FAILED` with requestId and server log detail (`KEY_VERSION_NOT_FOUND`, `AEAD_TAG_MISMATCH`, `CIPHER_FORMAT_INVALID`) without printing phone/key/ciphertext. Audit insert failure returns `AUDIT_LOG_UNAVAILABLE`. `db/verify/V20260628_verify_phone_privacy.sql` and `V20260628_002_clear_legacy_plain_phone.sql` now require full privacy coverage, not only `phone_hash`.
- 2026-06-30 single reveal AEAD repair: local manual click failure `requestId=d8df0ab6-3cbf-454c-9633-cdc0a94c4601` mapped to `surveyId=8` and `PHONE_DECRYPT_FAILED/AEAD_TAG_MISMATCH`. The row had complete privacy fields, `phone_enc_key_version=v1`, 12-byte IV, and 16-byte tag; the app loaded `v1`, so the practical local cause was stale ciphertext encrypted under a different `v1` key value, not missing columns or `KEY_VERSION_NOT_FOUND`. Before the write, a rollback SQL for overwritten privacy fields only, excluding plaintext phone, was saved at `.docker-tmp/phone-privacy-backups/survey-phone-privacy-before-rekey-20260630-134615.sql` with 53 rows and SHA-256 `608e68c1eb1846fbe846bbf7dbffd504e093b26fdc847be0350acf288b33c1ea`. Controlled rekey command `phone-privacy-backfill --table=survey --tenantId=default --batchSize=500 --dryRun=false --legacyDigits=true --rekey=true --confirmRekey=true` completed with `scanned=53 updated=53 invalid=0 skipped=0 legacy=52`. Post-write offline decrypt for `surveyId=8` succeeds, and MockMvc `POST /api/surveys/8/phone/reveal` returns HTTP 200 with `phoneRevealed=true` and `expiresInSeconds=60`. One row (`id=4`) still lacks `phone_suffix4_hash` because its legacy phone length is 1; do not run `V20260628_002_clear_legacy_plain_phone.sql` while legacy phone remains needed and this residual is understood.
- 2026-06-30 global phone display/system settings correction: the previous per-employee phone policy work was removed before its migration was run. Runtime no longer reads any employee policy table, no longer exposes per-user or batch phone-policy admin APIs, and `GET /api/users` returns only staff basics. New manual migration/verify files are `db/migrations/V20260630_001_tenant_system_settings.sql` and `db/verify/V20260630_verify_tenant_system_settings.sql` for one tenant/global `tenant_system_settings` row with `phone_display_policy` (`CLICK_TO_SESSION_VISIBLE` default, `MASKED_ONLY`) and `order_page_size` (1-100, default 20). Admins open the topbar/mobile-menu “设置” button to edit the system settings modal; employees have no settings entry and no self-service full-phone toggle. `POST /api/surveys/{surveyId}/phone/reveal` still first enforces row visibility and privacy-field readiness; on a successful visible-row click it activates the current user + current login session in `phone_reveal_login_session`, and subsequent list/detail reads for that user’s visible records use the global policy. Full phone values remain in frontend memory only, and full export still requires separate `PHONE_EXPORT_FULL` permission.
- 2026-06-30 phone privacy ON/OFF toggle: 系统设置 modal row "手机号策略" → "手机号隐私设置", dropdown replaced by a slide switch (ON=left/green=`CLICK_TO_SESSION_VISIBLE` session-wide; OFF=right/red=new `SINGLE_ORDER_TIMED_REVEAL`). OFF reveals only the clicked order for 300s, server-enforced by new table `phone_reveal_order_grant` (migration `db/migrations/V20260630_002_phone_reveal_order_grant.sql`, verify `db/verify/V20260630_verify_phone_reveal_order_grant.sql`, additive/idempotent). `MASKED_ONLY` removed from UI (enum kept @Deprecated). Backend: `PhoneRevealSessionService.activateOrder`/`revealedOrderExpiry`, reveal endpoint + `SurveyPhoneDisplayService.revealRows` branch on policy; `invalidateForTenant` also disables order grants on policy change. Row-visibility check still precedes any reveal; full phones live in frontend memory only and auto-remask at the per-row `phoneRevealExpiresAt`. Local `xyyx_db` has V20260630_002 applied. Verified `mvn test` + `npm test` (20/20). Bugfix: V20260630_001's CHECK `chk_tenant_system_phone_policy` only allowed the old two values, so saving OFF threw 500 (CHECK violation, not a 400 — proving the new backend was running). Corrective `db/migrations/V20260630_003_fix_phone_policy_check.sql` re-creates the CHECK with all three values (idempotent drop+add via prepared stmt); _001 + verify SQL updated for fresh installs. DB-only fix, no restart; an OFF write now passes.
- 2026-06-30 system-settings/phone-reveal migration applied locally: the live errors `保存设置 500 (INTERNAL_SERVER_ERROR)` and 小眼睛 `PHONE_SESSION_UNAVAILABLE` were missing-table issues, not code bugs. Local `xyyx_db` had only V20260628; V20260629 (`phone_reveal_login_session` + `personal_info_access_log` audit-extension columns) and V20260630 (`tenant_system_settings`) were unrun. Ran both (additive/idempotent, no DROP/RENAME, no row mutation); `db/verify/V20260629_*`/`V20260630_*` pass. JdbcTemplate has no schema cache so no backend restart was needed. Other environments must still run V20260629→V20260630 in order before enabling the 设置 modal / phone reveal.
- 2026-06-30 next follow-up date repair: `设置下次提醒` is not a notification system. The real behavior is `survey.next_survey_date`: new records derive it from `remindDays`, and overdue unprocessed rows show `需复访` in the list. `PUT /api/surveys/{id}/date` now requires the JWT current user, rejects missing/invalid/past dates, updates through tenant + admin/staff row scope, and returns JSON `success/message/nextSurveyDate/due`. The frontend now labels the field `下次回访日期`, explains that no system notification is sent, and shows saving/success/error state. Guarded by `SurveyReminderDateTests` and frontend `phonePrivacy.test.js`; verified with `mvn test`, `npm test`, and `npm run build`. Browser detail-flow QA was blocked by local Browser/mock-server constraints, so do not treat it as rendered acceptance.

- 2026-07-02 VPS 生产升级完成（8.156.93.144 / xujie.xin，宝塔原生部署，非 Docker Compose 栈）：本地构建（VPS 仅 1.8G 内存，不在机上构建）`mvn -DskipTests package` 的 jar 替换 `/www/wwwroot/xyyx-backend/xyyx-0.0.1-SNAPSHOT.jar`（systemd 服务名是 `spring_xyyx`，不是 vps-update.sh 默认的 xyyx-backend），前端按 `https://sso.xujie.xin` / `https://xujie.xin/auth/callback` 构建的 dist 替换 `/www/wwwroot/xyyx-front`。数据库按序执行了 V20260628_001、V20260629_001、V20260630_001/002/003、V20260701_001、V20260701_002；**跳过 V20260626_003**（其 P2 会恢复管理员已删通知，且 sys_role 已正确）；**V20260628_002（清明文 phone）仍未执行**，保持规约。**V20260701_001 在 VPS 上有偏差**：`user.uk_user_username` 被 VPS 独有外键 `notice_user_state.fk_notice_state_user`（引用 user.username，本地 schema 无此 FK）挡住无法 drop，处理为保留旧索引 + 照常新增 `uk_user_tenant_username` 与 `user_role.tenant_id`（单租户下全局唯一更严格，无代码影响）。新生产 `XYYX_PHONE_*` 密钥已用 openssl 生成并追加到 `/var/tmp/springboot/vhost/env/xyyx.env`（该文件同时含 DB/Casdoor 机密，勿打印；若宝塔面板重建 Java 项目可能覆盖此文件，需重新补密钥）。存量手机号回填：`phone-privacy-backfill --table=survey --tenantId=default --dryRun=false --legacyDigits=true` 结果 scanned=269 updated=269 invalid=0 legacy=2（275 行中 6 行 phone 为 NULL），coverage need_backfill=0。烟测：https 前端 200、`/api/me` 401、SSO discovery 200、服务 active。回滚件（非备份，是旧文件改名）：`xyyx-backend/xyyx-0.0.1-SNAPSHOT.jar.prev.20260702` 与 `/www/wwwroot/xyyx-front.prev.20260702`。用户明确要求跳过数据库备份、业务验证由用户自行进行——**登录/回访/手机号 reveal 的真实浏览器验收尚未做**。本机免密 SSH 仅在 PowerShell（Windows OpenSSH agent）下生效，Git Bash 连不上 agent 会报 publickey 拒绝。
- 2026-07-02 升级后"密码不对"事故排查：与升级无关。Casdoor 组织 `xyyx` 的 `password_expire_days` 被设为 3（6/28 建组织时误设），账号 `18007300157` 自 6/28 未改密码，7/1 起触发"密码已过期"拦截（Casdoor `record` 表证实：7/1 13:31 最后一次登录成功，7/2 23:24 报"您的密码已过期"；前三次"密码错误"是用户输错）。修复：casdoor 库 `organization.password_expire_days` xyyx 3→0，重置 `signin_wrong_times`，重启 casdoor 容器（app.conf 现在是正常文件，重启无 6/28 挂载问题），discovery 恢复 200。Casdoor 的 MySQL 库名 `xyyx_casdoor`，连接信息在 `/www/wwwroot/casdoor/conf/app.conf`（CRLF 行尾，脚本解析需 `tr -d '\r'`）。`built-in` 组织 expire=0 不受影响——Casdoor 控制台 admin 进不去与此无关（record 无 admin 尝试记录，疑似密码记错），未处理，等用户决定是否重置。
- 2026-07-02 第二拦截：登录报"Casdoor Token 未通过业务系统校验"。nginx 日志显示 `POST /api/auth/session` 返回 **403 / 20 字节** = Spring "Invalid CORS request"。根因：7/2 CORS 加固后白名单默认只有 localhost，而**"生产同源所以 CORS 不会触发"的旧结论是错的**——nginx 反代下后端看到的是 `http://127.0.0.1:8080`，浏览器 POST 自带 `Origin: https://xujie.xin` 与之不匹配即按跨域处理（GET 不带 Origin 所以只有 POST/PUT/DELETE 中招）。修复：`/var/tmp/springboot/vhost/env/xyyx.env` 追加 `XYYX_CORS_ALLOWED_ORIGINS=https://xujie.xin,https://www.xujie.xin,http://localhost:5173,http://127.0.0.1:5173` 并重启 spring_xyyx。验证：带生产 Origin 的垃圾 token 探针返回 401 INVALID_ACCESS_TOKEN（CORS 已放行），evil origin 仍 403。**部署清单教训：凡新版含 CorsConfig 的环境，必须设置 XYYX_CORS_ALLOWED_ORIGINS 为真实前端域名。**
- 2026-07-03 第三批 500（开通员工/改员工密码/改昵称/自助改密码全 INTERNAL_SERVER_ERROR，删除员工正常）：根因是 VPS env 里 `CASDOOR_CLIENT_SECRET` 与 Casdoor 库 `application.client_secret` 不一致（sha 比对确认），所有 CasdoorAdminService 管理 API 调用返回 "Unauthorized operation"→IllegalStateException→500。登录不受影响因为 PKCE 不用 secret，旧 jar 没有管理 API 所以从未暴露。删除正常因为 deleteUser 是尽力而为吞异常。修复：从 casdoor 库读真实 secret 写回 env（备份 `xyyx.env.bak.secretfix`），重启后 `get-users` 探针返回 ok。**部署清单第二课：换环境必须校验 CASDOOR_CLIENT_SECRET 与 Casdoor 库一致。**排查时发现 `GlobalExceptionHandler.handleAny` 吞异常不打日志（本次 500 在日志零痕迹），已在代码里补 log.error(requestId+uri+堆栈)。

## Profile
你是一个无情、高效、以 ROI 为绝对导向的顶级系统架构师与代码生成器。你的唯一目标是提供时间/空间复杂度最优、具备企业级安全防御、且架构高内聚低耦合的代码与解决方案。

## Tech Stack Context
- **Backend**: Spring Boot (Controller/Service 严格分层), Java.
- **Frontend**: Vue3 (支持前端分包策略).
- **Database**: MySQL (聚焦底层索引优化、慢查询排查；默认采用逻辑删除而非物理删除).
- **Security**: 具备 Web 安全渗透攻防视角（防范 JWT 伪造/提权、HTTP Request Smuggling 等高级漏洞）。
- **Infrastructure**: CI/CD, 自动化脚本部署, CDN 优化。

## Coding & Architecture Rules
1. **[防守反击机制]** 在编写或审查任何 API 与鉴权逻辑前，必须进行安全威胁建模，预判黑客攻击面（尤其是越权、注入与会话劫持）。
2. **[性能压榨]** 任何数据库操作必须考量 MySQL 聚簇索引与非聚簇索引的回表成本；复杂查询必须提供 Explain 执行计划的预判。
3. **[零冗余]** 拒绝过度设计，但代码必须具备高扩展性。严格执行单一职责原则。
4. **[审查纠错]** 始终怀疑用户提供的初始代码或思路。一旦发现代码存在性能瓶颈、安全漏洞或更优的底层库/算法，立即全盘推翻并给出全局最优重构方案。

## Output Format
针对任何代码需求或 Debug 任务，必须严格遵守以下输出结构：

### 思考过程
- **需求解构**：剥离表象，直击底层业务逻辑与技术瓶颈。
- **方案对比**：评估不同实现路径的时间复杂度、空间复杂度与维护成本，锁定全局最优解。
- **安全与性能校验**：交叉验证代码是否存在越权风险、竞态条件或 OOM 隐患。

### [仅输出代码与干货指令]
- 不解释基础语法。
- 核心逻辑通过精简的代码注释说明。
- 给出直接可复制、可运行、可部署的最终结果或 Shell 脚本。
